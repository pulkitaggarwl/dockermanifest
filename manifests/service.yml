apiVersion: v1
kind: Service
metadata:
    name: "jysinaks2-549f"
    labels:
        app: "jysinaks2-549f"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "jysinaks2-549f"
